# Example A exploration

Place dataset inspection, distributions, visualizations, and temporary hypotheses here. Use numbered, descriptive notebook names such as `01_inspect_labels.ipynb`. Move reusable logic into `src/project/`.

