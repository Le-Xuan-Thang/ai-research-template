# Example B dataset

Second placeholder dataset showing that one project and one experiment can span multiple independent datasets. Replace this file with source, citation, version, license, download, preprocessing, layout, split, and caveat information for the real dataset.

Treat `raw/` as immutable. Use `interim/` for incomplete transformations and `processed/` for experiment-ready inputs.

