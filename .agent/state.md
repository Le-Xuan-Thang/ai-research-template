# Current research state

## Objective

Maintain `ai-research-template` as a lightweight, public, reusable, multi-dataset AI/ML research project template.

## Known state

- The template structure is established; no real research question, datasets, or executed experiments have been registered.
- `project` is a placeholder Python package name.
- `example_a`, `example_b`, and `exp001_baseline` are instructional metadata only.
- English documentation is canonical; Vietnamese translations are aligned for human-facing guides.
- Python setup is `uv`-first with Python 3.10 pinned in `.python-version`; standard `venv`/`pip` remains a fallback.
- `paper/` contains the existing Elsevier CAS assets and local `write-ai-paper` skill. The complete manuscript belongs in the single `paper/main.tex`, with publication figures in `paper/figs/`.
- `website/` is a dependency-free project dashboard and `CHANGELOG.md` records notable releases and updates.
- The public repository identity is `Le-Xuan-Thang/ai-research-template`; MIT is the canonical project license, with third-party Elsevier assets retaining their own notices and terms.
- GitHub-facing contribution and citation metadata are available in `CONTRIBUTING.md` and `CITATION.cff`.
- `AGENTS.md` requires proactive handoffs before context, quota, or long-running work can outlive a session.

## Active work

No research experiment is active.

## Next action

Define the real research question, rename the package, replace example dataset records, and create the first real configuration snapshot before running an experiment.
